---
import {cn} from '~/utils/merge.ts'
const {class: className} = Astro.props
---

<h1
        class={cn('mb-[0.4rem] font-junicode font-semibold leading-[0.9] text-dark', className)}
>
    <slot/>
</h1>
 